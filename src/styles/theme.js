import { colors } from './colors';
import { metrics } from './metrics';
import {fonts, fontSizes} from "./fonts";
import {profileSize} from "./sizes";

export const theme = {
    colors,
    metrics,
    fonts,
    fontSizes,
    profileSize
};
