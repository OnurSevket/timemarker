import React from "react";
import { ActItemTitleText, FlexBox } from "../../atoms";

export const LoginScreen = () => {
  return (
    <FlexBox flex={1}>
      <ActItemTitleText>Auth Screen</ActItemTitleText>
    </FlexBox>
  );
};
